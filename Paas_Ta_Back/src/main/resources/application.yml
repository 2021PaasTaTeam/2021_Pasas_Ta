spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gathermarket
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
  messages:
    basename: errors

  mail:
    host: smtp.gmail.com
    port: 587
    username: joohyun8468@gmail.com
    password: kkkoh2727!
    properties.mail.smtp:
      auth: true
      starttls.enable: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

server:
  servlet:
    session:
      tracking-modes: cookie
      timeout: 10